'use client'

import { AccountMemberGrid } from '@/app/dashboard/account/account-member-grid'

/** Member account: Saved Sessions + Session History. */
export function MemberWorkspacePanels() {
  return <AccountMemberGrid />
}
